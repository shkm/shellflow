{
  "$schema": "../src-tauri/src/config.schema.json",
  "tasks": [
    {
      "name": "Dev",
      "command": "VITE_PORT={{ branch | hash_port }} npm run tauri dev",
      "kind": "daemon",
      "shell": "/bin/bash",
      "urls": {
        "Vite": "http://localhost:{{ branch | hash_port }}"
      }
    },
    {
      "name": "Build",
      "command": "npm run tauri build",
      "kind": "command",
    },
  ],
}
